% TikZ関連
\usepackage{genealogytree} % 系統樹図
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{circuitikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{tikz-3dplot}

\usetikzlibrary{
  arrows.meta,
  positioning,
  calc,
  shapes.geometric,
  shapes.symbols,
  decorations.pathmorphing,
  decorations.markings,
  backgrounds,
  intersections,
  patterns,
  angles,
  quotes,
  3d
}

% 基本スタイル（アーム・制御系など）
\tikzstyle{block} = [draw, fill=blue!10, rectangle, minimum height=3em, minimum width=4em, thick]
\tikzstyle{sum}   = [draw, circle, thick]
\tikzstyle{joint} = [circle, draw, fill=black!80, minimum size=5pt, inner sep=0pt]
\tikzstyle{link}  = [thick, line cap=round]
\tikzstyle{end}   = [circle, draw=red!70!black, fill=red!30, minimum size=6pt, inner sep=0pt]

% 座標軸や背景などに使える軽量スタイル
\tikzstyle{axis} = [->, very thick]
\tikzstyle{vector} = [->, thick, >=stealth]

% --- 制御系ブロック線図スタイル ---
\tikzset{
  block/.style = {draw, fill=blue!10, rectangle, minimum height=3em, minimum width=4em, thick},
  sum/.style   = {draw, circle, thick, node distance=2cm, inner sep=0pt, minimum size=6mm},
  input/.style = {coordinate},
  output/.style = {coordinate},
  link/.style  = {->, thick, >=Stealth}
}
